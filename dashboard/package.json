{
  "name": "qr-swarm-dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend for the QR swarm dashboard (compiles src/*.ts -> static/*.js)",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
