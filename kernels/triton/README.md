# Triton Kernel Templates

This directory is for Triton experiments. Triton is not a required local
dependency because it is not useful on this Mac without an NVIDIA GPU.

Use this path for small kernels, classifiers, or trailing-update experiments
that can be launched from Python on a CUDA machine. Anything promoted to the
final Popcorn submission must be packed into the single `submission.py` file.
